# Ignore sensitive files
.env
.env.template

# Ignore only large or redundant directories
node_modules/
.DS_Store

# Logs & Snapshots (keep CLI scripts for development)
logs/
snapshots/*.log

# Keep system and CLI files for testing
# (systemIndex.json, versions.json, CLI scripts will remain tracked)

# IDE metadata
.vscode/
