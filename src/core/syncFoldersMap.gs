/**
 * פונקציה זו הושהתה זמנית ולא תרוץ עד שתחזור לפיתוח.
 * מטרתה לסנכרן בין FoldersMap (Active) לנתוני folderbuilder_summary.json.
 * כרגע לא מופעלת כדי לא לעכב את ההתקדמות.
 */
function syncFoldersMapFromSummary() {
  console.log(
    "⚠️ פונקציית syncFoldersMapFromSummary מושבתת זמנית. לא מתבצע סנכרון."
  );
  return;
}
